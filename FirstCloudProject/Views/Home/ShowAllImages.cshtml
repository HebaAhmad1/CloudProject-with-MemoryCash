@model List<Image>
    <div class="py-5 ">
    <div class="container">

        <h3 class="TitleName" style="margin-top: -30px;"> All Images</h3>
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-md-4">
                    <div class="card mb-4 shadow-sm">
                        <img src="@item.ImagePath" class="img-thumbnail" />
                        <div class="card-body">
                            <h3 class="card-title">@item.Key</h3>
                        </div>
                    </div>
                </div>
            }
        </div>
        </div>
</div>